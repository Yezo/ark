export default function ProjectSectionSubtitle({ children }: { children: React.ReactNode }) {
  return (
    <p className="max-w-[50ch] text-md tracking-wide text-[0.85rem] 2xl:max-w-[65ch]">{children}</p>
  )
}
